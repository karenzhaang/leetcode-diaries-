Kadane's algorithm is an efficient optimization technuqur used to find the maximum sum of a contiguous subarray withon a one- dimentional array of numbers


def max_subarray(nums):
    max_sum + nums[0]
    current_sum = num[0]

for i in range(1, len(nums)):
    current_sum = max(nums[i], current_sum + nums[i])

    max_sum = max(max_sum, current_sum)

return max_sum
